function renderProgress(){
 let workouts=[...state.workouts].sort((a,b)=>b.date.localeCompare(a.date)),ex=workouts.reduce((n,w)=>n+w.exercises.length,0),groups=new Set(workouts.flatMap(w=>w.muscles)).size;
 document.getElementById("progressView").innerHTML=`<div class="progress card"><h2 style="margin:0">Training Overview</h2><div class="muted">Your logged workout history.</div><div class="stats"><div class="stat"><span class="muted">Workouts</span><strong>${workouts.length}</strong></div><div class="stat"><span class="muted">Exercises</span><strong>${ex}</strong></div><div class="stat"><span class="muted">Muscles</span><strong>${groups}</strong></div></div></div><div class="progress card" style="margin-top:16px">${workouts.length?workouts.map(w=>`<div class="exercise-row"><div><strong>${new Date(w.date+"T00:00:00").toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"})}</strong><div class="muted">${w.muscles.map(muscle).join(" · ")}</div></div><span>${w.exercises.length}</span></div>`).join(""):`<div class="empty">No workouts logged yet.</div>`}</div>`
}
