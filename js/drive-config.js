/* Google Cloud → APIs & Services → Credentials → OAuth client (Web).
   Authorized JavaScript origins: the GitHub Pages origin.
   Authorized redirect URIs: this app's URL, including the trailing slash.
   Scope used: drive.file (only files this app creates) + email.
   Leave empty to paste a client ID on first Connect. */
const GOOGLE_DRIVE_CLIENT_ID="";
