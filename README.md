# Workout Tracker

Replace the existing `index.html` in the GitHub repository with the included file.

The footer/navigation has been given more vertical space and bottom safe-area padding for iPhone, and the floating + button has been moved slightly upward.

Files:
- index.html — main app
- manifest.webmanifest — PWA manifest
- sw.js — service worker
